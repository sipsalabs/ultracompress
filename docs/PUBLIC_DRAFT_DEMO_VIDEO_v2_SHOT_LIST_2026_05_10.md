# Demo Video v2 — Shot List + B-Roll Spec

**Solo, OBS, no editor.** Runtime 3:30-4:00 (cap 5:00). Sip pre-flight to YouTube Public: ~90 min. Charter: results-only, zero recipe values.

## 1) Pre-flight (60 sec)

- [ ] OBS: MP4, 1920x1080, 30fps, CBR 12 Mbps, Display Capture source.
- [ ] Mic: "level check at 0 dB" -- peak below -6 dB.
- [ ] Layout: Terminal LEFT half (JetBrains Mono 18pt, plain `$ `, scrollback cleared); Browser TOP-RIGHT = HF model card; BOTTOM-RIGHT = `github.com/sipsalabs/ultracompress`.
- [ ] Slack/Discord/Outlook quit; taskbar auto-hide; Focus Assist = Alarms only.
- [ ] Clean venv with `pip install ultracompress` already done. Verify: `uc --version`.

## 2) Shot list (10 shots, ~3:35)

Each shot = one OBS recording (`shot_NN.mp4`). Concatenate at end. NO face cam (Shot 2 has optional face variant). VO lines in Section 4.

| # | Sec | Setup | Action | VO | Cut |
|---|---|---|---|---|---|
| 1 | 10 | `title.png` (white-on-black) full-screen in Photos | Display 10s | L1 | 10.0s |
| 2 | 20 | **Rec:** black screen, voice only. **Opt:** webcam upper-third, dark wall, overhead lamp | Speak | L2 | last word |
| 3 | 15 | Terminal, fresh `$ ` | Type `uc --version` slowly, Enter -> `ultracompress 0.5.5` | L3 | 1s after print |
| 4 | 30 | Terminal, fresh prompt | `uc verify SipsaLabs/qwen3-1.7b-base-uc-v3-bpw5`, Enter. Cache: <2s; cold: ~30s | L4a during DL, L4b on PASS | 1.5s after green PASS |
| 5 | 30 | Terminal, fresh prompt | `uc inspect SipsaLabs/qwen3-1.7b-base-uc-v3-bpw5`, Enter -> per-layer SHA table scrolls | L5 | 1s after table |
| 6 | 20 | Click TOP-RIGHT (HF card) | Slow-scroll to `PPL ratio: 1.00401x`, hover the cell | L6 | when VO ends |
| 7 | 30 | Terminal; `gen.py` saved (snippet below) | `python gen.py`, Enter -> ~8s -> real output | L7 | 2s after gen |
| 8 | 20 | Click BOTTOM-RIGHT (GitHub) | Open `BENCHMARKS_2026_05_10.json`, Ctrl+F `verified_records`, scroll 16-entry array | L8 | end of array |
| 9 | 15 | Terminal, Ctrl+L | Type slowly: `pip install ultracompress` then `uc verify ORG/MODEL`. DON'T press Enter | L9 | 1s after VO |
| 10 | 10 | `endcard.png` ("sipsalabs.com/inference" + "Free $5 credit on signup") | Full-screen in Photos | L10 | 10.0s, hard cut |

`gen.py`:
```python
from transformers import AutoModelForCausalLM, AutoTokenizer
m = AutoModelForCausalLM.from_pretrained("./demo_pack")
t = AutoTokenizer.from_pretrained("./demo_pack")
print(t.decode(m.generate(t("Hello world", return_tensors="pt").input_ids, max_new_tokens=20)[0]))
```

## 3) B-roll (2 OPTIONAL)

1. `nvidia-smi.mp4` (5s): hold dual-5090 output. Insert Shot 7 -> 8 if pacing drags.
2. `code_visual.mp4` (3s): VS Code static frame on `ultracompress/inference.py`, cursor visible. Insert Shot 5 -> 6.

## 4) Voiceover (verbatim, ~395 words, ~3:00 at 130 WPM)

**L1.** *Lossless 5-bit transformer compression. SHA-256 verifiable. Open source verifier.*

**L2.** *I'm going to show you how to verify lossless compression on your own machine in thirty seconds. No notebook trust. SHA-256 against the published manifest -- if a single bit drifts, you'll see it.*

**L3.** *Pre-installed in a clean venv -- pip install ultracompress takes about ten seconds the first time. Skipping that here so we stay under three minutes.*

**L4a.** *One command. It pulls the pack from Hugging Face, recomputes SHA-256 over every shard, and checks the manifest. Watch the last line.*

**L4b.** *VERIFY: PASS. Lossless reconstruction guaranteed. Bit-identical to what the trainer produced.*

**L5.** *Inspect prints the per-layer hashes. Every transformer block, every projection -- each row is a SHA-256 you can recompute. The manifest is signed and pinned to the pack version. This is what verify checks against.*

**L6.** *Model card on Hugging Face. Perplexity ratio one point zero zero four oh one against stock Qwen3-1.7B base. Same number you'll get if you re-run the eval. PPL ratios match the published BENCHMARKS dot J-S-O-N. Open the verifier on your own machine.*

**L7.** *Loads with stock transformers -- no custom loader, no monkey-patch. Same generate API you already use. The output is real, generated by the 5-bit pack on this GPU.*

**L8.** *Fifteen verified records. Each entry: model ID, bpw, perplexity ratio, manifest SHA-256. Public file, public commit history. Diff it against any release tag.*

**L9.** *Try it. Pip install ultracompress. uc verify org slash model. Open verifier at github.com slash sipsalabs slash ultracompress.*

**L10.** *sipsalabs.com slash inference. Free five-dollar credit on signup. Try it. Twitter at SipsaLabs. Email founder at sipsalabs dot com.*

**Charter:** zero recipe values. Honest disclaimer in L6. Single ASK in L10. No "Hi guys / what's up YouTube." No "like and subscribe."

## 5) Post-production (~25 min)

**Concat:** OBS Tools -> Combine, OR Photos "Make a video".

**Title:** `Lossless 5-bit transformer compression you can verify yourself`
- A/B Alt 1: `5-bit. Lossless. Verified. UltraCompress in 30 seconds.`
- A/B Alt 2: `Verify lossless transformer compression on your own GPU`

**Description (under 200 words, paste verbatim):**
```
Lossless 5-bit transformer compression with SHA-256 verifiable manifests. One command to verify any pack on your own GPU.

  pip install ultracompress
  uc verify SipsaLabs/qwen3-1.7b-base-uc-v3-bpw5

* Stock transformers loader -- no monkey-patch
* Per-layer SHA-256 in every manifest
* 15 verified records published, version-pinned
* Apache-2.0 verifier, public benchmark file
* PPL ratio 1.00401x on Qwen3-1.7B-Base at 5 bpw

Inference API:     https://sipsalabs.com/inference
Verifier (OSS):    https://github.com/sipsalabs/ultracompress
Pre-packed models: https://huggingface.co/SipsaLabs
Benchmarks:        https://github.com/sipsalabs/ultracompress/blob/main/docs/BENCHMARKS_2026_05_10.json
Why 5-bit:         https://sipsalabs.com/blog/empty-5bit-band

0:00 Intro
0:30 Verifier flow
2:00 BENCHMARKS scroll

Sipsa Labs. founder@sipsalabs.com. Twitter @SipsaLabs.
```

**Thumbnail:** 1280x720 PNG, black `#000000`, white Inter Bold 120pt "5-bit. Lossless. Verified." three lines centered. No face, no logo. MS Paint or Canva, 5 min.

**Tags:** `llm, quantization, compression, ai-infrastructure, lossless`

**Upload:** YouTube Studio -> drag MP4 -> metadata -> thumbnail -> Public, embedding ON, comments ON, Science & Technology, Standard License.

---

*v2. Locked 2026-05-10 for Mon 5/11 HN launch.*
