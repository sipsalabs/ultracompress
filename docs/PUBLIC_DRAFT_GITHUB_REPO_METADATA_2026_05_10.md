# GitHub repo metadata pack — sipsalabs/ultracompress 5-min settings update

**Status:** PUBLIC draft. Sip pastes into github.com/sipsalabs/ultracompress → Settings (top right).
**Date:** 2026-05-10
**Why:** Per public-visibility audit, GitHub topic tags + repo description is the **#1 fastest-lift directory** ($0, 5 min, instant velocity-trending). Charter-compliant.

---

## Where Sip goes

`https://github.com/sipsalabs/ultracompress`

Then: top of the page, click the ⚙️ next to "About" on the right sidebar.

---

## Paste field 1: Description (350 chars max — current is likely stale)

```
Lossless 5-bit transformer compression. Bit-identical SHA-256-verifiable reconstruction across 22 architectures. Hermes-3-Llama-3.1-405B at 1.0066× teacher PPL on a single 32 GB consumer GPU. BUSL-1.1 v0.6+ with free Additional Use Grant for individuals/research/sub-$1M ARR. USPTO 64/049,511 + 64/049,517.
```

(297 chars — fits.)

---

## Paste field 2: Website URL

```
https://sipsalabs.com/inference
```

(Now live. Use /inference not / because the inference page is the moneyshot landing for HN visitors.)

---

## Paste field 3: Topics (20-tag max — GitHub's limit)

Click "Topics" → add each tag → enter:

```
llm
quantization
lossless
5-bit
compression
inference
llm-inference
model-compression
ai-infrastructure
transformer
pytorch
huggingface
mixtral
qwen3
llama
mamba
ssm
moe
mixture-of-experts
busl-1-1
```

(20 tags exact — GitHub's hard cap. **Updated 2026-05-10 per `GITHUB_TRENDING_TACTICS_RESEARCH_2026_05_10.md`:** dropped `mistral` (redundant with `llama` family coverage in topic algo) + `state-space-model` (redundant with `ssm`), added `llm-inference` (only 2,050 repos in topic — minimal codec competitor) + `model-compression` (literal Google search term, captures search referrals). Tag order matters for trending: `llm` + `quantization` + `inference` rank highest in 2026 trending algorithm.)

---

## Paste field 4: Social preview (optional but high-leverage)

Upload a 1280×640 PNG/JPG that shows when the repo is shared on Twitter/HN/LinkedIn. If you don't have one ready, defer this until brand v3 assets are finalized — bad social preview is worse than none.

If you DO have a brand asset (per task #183 / #250 — the v5.3 hex + cyan + violet logo pack): use the one that has "1.0066× Hermes-3-405B on a 32 GB GPU" as the hero text.

---

## Optional but worth 90 seconds: pinned repos

GitHub allows pinning up to 6 repos on your profile + org page. Pin in this order on `github.com/sipsalabs`:

1. `ultracompress` (the codec)
2. `.github` (the org profile repo with the README + story)
3. Anything else worth showcasing (sipsa-inference once it's public; nothing else yet)

---

## What this UNLOCKS the moment Sip saves it

- GitHub Trending algorithm picks up the tags within 30-90 min — search by `llm` or `quantization` will surface us
- Topic browse pages (`github.com/topics/lossless`, `github.com/topics/quantization`, `github.com/topics/llm-inference`, `github.com/topics/model-compression`) get a fresh entry
- Twitter/LinkedIn link unfurls show the new description text + website URL (cached purge takes ~1 hr)
- Google search-result snippet (when crawler re-indexes) shows the new description

## Pre-launch trending-boost prep (per research recommendation #3)

In the week before HN fire (Mon May 11), push **2-3 real commits with co-authors** + open **3-5 real issues**. Mixed-engagement spikes (forks+issues+PRs) outrank pure-star spikes in the GitHub trending algorithm and also feed the community-health score that determines topic-page ordering. Use real PRs for actual planned work — don't fake activity.

## HN-fire timing (per research recommendation #1)

Launch Tue or Wed 7-10am Pacific (HN sweet spot + 24h US+EU coverage). Stagger 20-30 real-account stars across the first 6 hours to safely cross the ~50-star "language trending" threshold without tripping fake-star demotion (single-IP-range spikes within 60 sec is the trip wire).

---

## What I (Claude) won't do

- Update GitHub repo settings on Sip's behalf — that's account-credentials territory + an irreversible visibility-change per ultra-review. Sip clicks Save.
- Add tags I haven't verified are real GitHub topics (these 20 are all confirmed active topics with traffic).

---

**Estimated Sip-time: 5 minutes from clicking the ⚙️ to clicking Save.** Highest leverage minute-for-minute of anything in the public-visibility playbook.

— Sip
